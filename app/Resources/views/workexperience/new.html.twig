{% extends 'tamplate.html.twig' %}

{% block body %}
    <h1>Workexperience creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-default"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('workexperience_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
